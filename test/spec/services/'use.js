'use strict';

describe('Service: 'use', function () {

  // load the service's module
  beforeEach(module('bagOFoodApp'));

  // instantiate service
  var 'use;
  beforeEach(inject(function (_'use_) {
    'use = _'use_;
  }));

  it('should do something', function () {
    expect(!!'use).toBe(true);
  });

});
